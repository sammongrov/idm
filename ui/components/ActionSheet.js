/**
 * @flow
 *
 * The ActionSheet component wraps the `react-native-actionsheet` ActionSheet component to allow you
 * to easily replace this with an alternative ActionSheet implementation if required
 */
import ActionSheet from 'react-native-actionsheet';

export default ActionSheet;
